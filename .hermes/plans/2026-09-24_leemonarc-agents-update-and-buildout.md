# Lee Monarc — AGENTS.md Update + Revised-Site Build-Out Plan

**Goal:** (A) surgically update AGENTS.md to reflect the revised copy deck and verified repo reality; (B) build out the remaining 9 marketing pages + /privacy from `docs/LeeRevised September 2026.docx` using a herdr fleet with disjoint write surfaces.

**Architecture:** Next.js 16.3.5 / React 19.3 / TS, Server Components, no animation framework (native sticky + passive scroll per HANDOFF). Orchestrator (this session) is the single writer to shared files; fleet agents each own exactly one route folder + one receipt file.

**Verified context (2026-09-24):**
- Routes built: only `/` and `/contact`. Copy deck defines 11 marketing pages + /privacy.
- Scripts: `dev`/`build`/`start` pinned to 127.0.0.1:3100; `lint` (eslint 9.39.5 pinned); `typecheck`; `test` = Playwright against a running server (`PLAYWRIGHT_BASE_URL` retargets); `doctor` = react-doctor; `node scripts/verify-visual.mjs` captures production on 3101 into `artifacts/production/`.
- Business spelling is **Lee Monarc** (HANDOFF + package name); current AGENTS.md says "Lee Monarch" throughout — stale.
- Copy deck facts marked UNCONFIRMED in its own Review Notes: pricing, client proof/testimonials, Chartered Accountant designation, 13 years experience, vivienne@leemonarc.com.au, first-person founder quotes, privacy policy content.
- Hazards: `src/app/oracleJdk-27/` = stray untracked 370MB JDK inside the app dir; both `bun.lock` (untracked) and `package-lock.json` present (npm is canonical per README); Windows npm wrapper can orphan a Node child holding port 3100/3101.

---

## Track A — AGENTS.md surgical update (orchestrator, ~1 pass)

Merge discipline: preserve all existing sections verbatim (Brand Authority, Creative Reference, Design Standard, Interaction/Motion, Technical Direction, Editability, Design Authority, Imagery, Working Method, Visual Verification, Production Floor, nextjs-agent-rules block). Only these edits:

### Task A1: Fix stale spelling
- Replace all `Lee Monarch` → `Lee Monarc` in AGENTS.md (title + Mission).

### Task A2: Replace "Site Architecture" guesswork with deck IA
- Replace the "Likely surfaces include: Home / Services / Advisory / About / Contact — not mandatory" block with the deck's approved IA (paths extracted verbatim from the deck):
  - `/` Home · `/services` · `/who-we-help` · `/about` · `/contact`
  - `/services/accounting-compliance` · `/services/tax-planning` · `/services/business-structuring` · `/services/fractional-cfo-advisory` · `/services/business-acquisition` · `/services/succession-exit`
  - `/privacy` planned, blocked on legal content
- Note: only `/` and `/contact` exist today.

### Task A3: Content section — point at the deck
- Add: current copy source is `docs/LeeRevised September 2026.docx` (Sep 23). Its Section 13 "Review Notes" lists unconfirmed facts — pricing, client proof, credentials, contact email, founder first-person quotes. Do not present these as established until Vivienne confirms.

### Task A4: Project Documentation — add the deck to the read-first list.

### Task A5: Add "Commands" section (verified from package.json/README)
- `npm ci` / `npm run dev` (http://127.0.0.1:3100) / `npm run build` / `npm start` (3100; stop dev first)
- `npm run lint`, `npm run typecheck`
- `npm test` — needs a running server on 3100; `PLAYWRIGHT_BASE_URL=http://127.0.0.1:3101 npm test` targets production
- `node scripts/verify-visual.mjs` — production captures on 3101

### Task A6: Add "Pitfalls" section
- `src/app/oracleJdk-27/`: stray 370MB JDK, untracked — not source, exclude from searches; deletion candidate after user confirms
- Dual lockfiles: `package-lock.json` is canonical; `bun.lock` is untracked residue — use npm
- ESLint pinned at 9.39.5 (eslint 10 breaks the Next plugin); upgrade the pair together
- Windows: killing an npm wrapper can orphan the Node child on 3100/3101 — verify port free before restart; HTTP 200 from `/` is not proof (check CSS/JS asset responses)
- New machine: `PLAYWRIGHT_SKIP_BROWSER_GC=1 npx playwright install chromium`
- Copy-deck filename contains a space (`docs/LeeRevised September 2026.docx`) — quote the path in every script and agent prompt
- Keep the `nextjs-agent-rules` block — regenerated by `next dev`

**Validation A:** file reads clean, no command appears that isn't in package.json/README, `git diff AGENTS.md` shows only the edits above.

---

## Track B — Fleet build-out of remaining pages (after Track A)

### Task B0 (orchestrator): Prepare shared scaffolding FIRST, before any fan-out
- USER CONFIRMATION NEEDED first: delete/move `src/app/oracleJdk-27` (370MB untracked JDK) and decide the lockfile (delete `bun.lock`; npm is canonical per README)
- Routes are nested: service pages live under `src/app/services/<slug>/page.tsx` (deck-specified paths, see A2)
- Extend `src/content/site.ts` with per-page content slots (orchestrator owns this file exclusively — it is the #1 contention point)
- Update `src/components/chrome.tsx` nav/footer once (orchestrator) to the deck's Section 12 shared nav
- Create `artifacts/receipts/` + receipt schema: page slug, files written, copy source (deck-verbatim vs provisional), widths visually verified, test command, DONE counts
- `git commit` this scaffold so agents branch from a stable base

### Task B1..B8 (fleet, one pane per page, parallel after B0)
- Home `/` is a REVISION (add services list + owner-questions sections to the existing reference implementation), not greenfield
- New pages: `/services`, `/who-we-help`, `/about`, and the six `/services/<slug>` detail pages
- `/contact` exists — only verify it survives the nav change
- `/privacy` is BLOCKED (HANDOFF flags privacy wording as a launch decision) — stub route only, clearly flagged

Each agent's contract (template: herdr-fleet-ops `references/delegation-contracts.md`):
- ONLY write `src/app/<route>/page.tsx` (+ `<route>.css` if needed) and one receipt `artifacts/receipts/<route>.md`
- Copy comes from the deck (orchestrator pastes that page's copy into the prompt — agents do NOT read the docx, saves tokens and avoids extraction issues)
- FORBIDDEN: `site.ts`, `chrome.tsx`, `tokens.css`, `globals.css`, other routes, git, shared indexes
- Follow existing patterns: Server Component, no `use client` on whole page, semantic HTML, tokens from `tokens.css` (no local color/type overrides), reduced-motion safe
- Placeholder (clearly marked HTML comment) for unconfirmed facts — never invent pricing/testimonials/credentials
- Receipt must record which copy is deck-verbatim vs provisional (client review targets the provisional text)
- Terminate with DONE + line counts

### Task B10 (orchestrator): Integrate + verify
- Harvest receipts, collate, flag contradictions
- `npm run typecheck && npm run lint && npm run build`
- Dev server + visual pass at 360/768/1440 both still and through motion states
- `npm test` (extend Playwright specs per page — tests cover nav, a11y via axe, no-JS, reduced-motion)
- Update `docs/HANDOFF.md` with the new page map
- Commit per page-batch

---

## Files likely to change
- `AGENTS.md` (Track A)
- `src/content/site.ts`, `src/components/chrome.tsx` (B0, orchestrator only)
- `src/app/<route>/page.tsx` ×9–10 (fleet, disjoint)
- `tests/*.spec.ts` (B10)
- `docs/HANDOFF.md` (B10)

## Risks / open questions
1. **Shared-file contention** — `site.ts`/`chrome.tsx` touched by nobody but the orchestrator; enforce in contracts.
2. **Unconfirmed copy facts** — deck Section 13 items must ship as marked placeholders or be omitted; confirm with Vivienne before launch claims.
3. **Stray JDK in `src/app`** — may slow Next's file watching/build; recommend deleting or moving `src/app/oracleJdk-27` before the build-out (needs user confirmation — 370MB, untracked).
4. **Fleet model quality** — free-tier panes (solar-pro4) are fine for scaffolded page assembly with pasted copy, not for design judgment; orchestrator reviews every receipt against DESIGN.md/MOTION.md.
5. Deck is .docx — orchestrator extracts page copy once and distributes; do not let N agents each parse 53KB of Word XML.
