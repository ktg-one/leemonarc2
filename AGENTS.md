# Lee Monarc

The earlier architectural homepage was rejected. Do not rebuild it, and do not restore its headline, pinned perspective scene, or monogram study.

The current site is this folder. Build it feature-by-feature from Paraform (https://www.paraform.com/): wide video hero, light chapters, then the dark card half, then a light close. Figma file `LeemonArc` node `1:18` supplies layout slots. Correct extraction defects. Do not copy Paraform's recruiting copy, clients, staff, or statistics.

Words come from `docs/LeeRevised September 2026.docx` and `artifacts/copy/`. Facts the deck marks unconfirmed stay labelled. Missing video, portrait, logos, and proof stay labelled placeholders. Keep the existing routes, email, and phone. No fake form success and no public publishing.

Homepage assembly: `src/app/page.tsx` (`Hero`, `LightSections`, `DarkSections`). Tokens: `src/app/tokens.css`. Shared shell: `src/app/refresh.css`.
